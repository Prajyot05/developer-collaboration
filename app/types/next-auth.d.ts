import NextAuth from "next-auth";

console.log("NextAuth loaded", NextAuth);

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      name?: string | null;
      email?: string | null;
      image?: string | null;
    };
  }
}
